<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %> <!-- gravatar_for is in helpers -->
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>
	


